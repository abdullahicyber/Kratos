<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🔑 Add permissions OUTSIDE the <application> tag -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/kratos"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kratos_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kratos">

        <!-- Launcher (first screen) -->
        <activity android:name=".MainActivity" />

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- 🔧 Add your CreateProfileActivity HERE (anywhere inside <application>) -->
        <activity android:name=".CreateProfileActivity" />

        <!-- Other activities in the same <application> block -->
        <activity android:name=".SignInActivity" />
        <activity android:name=".RegisterActivity"
            android:label="Create Account" />
        <activity android:name=".ChatActivity" />

    </application>

</manifest>
